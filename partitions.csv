# Custom partition table for the GoPro Remote.
# The BLE (Bluedroid) stack makes the application image bigger than 1 MB,
# so the default "single factory app" table no longer fits. This table keeps
# the classic ESP-IDF layout but grows the factory app partition to 1.9 MB.
#
# Name,      Type, SubType, Offset,   Size,     Flags
nvs,         data, nvs,     0x9000,   0x6000,
phy_init,    data, phy,     0xF000,   0x1000,
factory,     app,  factory, 0x10000,  0x1F0000,
storage,     data, spiffs,  0x200000, 0x200000,
