# ESP-IDF project root CMakeLists.
# All build logic lives in $IDF_PATH/tools/cmake/project.cmake.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(gopro_remote)
